# auto-generated by grc.converter

id: weatherstation_encode_ws
label: WS Encoder
category: '[weatherstation]'

parameters:
-   id: sample_rate
    label: Sample Rate
    dtype: int
    default: '2500000'
-   id: pulse_length_ms
    label: Pulse length [ms]
    dtype: float
    default: '0.5'
-   id: debug
    label: Debug
    dtype: bool
    default: 'False'
    options: ['True', 'False']
    option_labels: [Enable, Disable]

inputs:
-   domain: message
    id: in

outputs:
-   domain: stream
    dtype: float

templates:
    imports: import weatherstation
    make: weatherstation.encode_ws(${sample_rate}, ${pulse_length_ms}, ${debug})

file_format: 1
